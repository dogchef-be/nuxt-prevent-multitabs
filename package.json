{
  "name": "nuxt-prevent-multitabs",
  "version": "1.0.5",
  "description": "NuxtJS module to prevent opening the app in multiple tabs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dogchef-be/nuxt-prevent-multitabs.git"
  },
  "license": "MIT",
  "contributors": [],
  "files": [
    "dist"
  ],
  "main": "dist/module.js",
  "types": "dist/module.d.ts",
  "scripts": {
    "clean": "npm prune && rm -rf dist",
    "transpile": "tsc",
    "build": "npm run clean && npm run transpile"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@nuxt/types": "^2.18.1",
    "@nuxt/typescript-build": "^2.1.0",
    "nuxt": "^2.18.1",
    "typescript": "^4.9.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
